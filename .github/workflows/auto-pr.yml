name: Auto Create PR on Branch Push

on:
  push:
    branches-ignore:
      - main
      - master

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if PR exists
        id: check-pr
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          PR_COUNT=$(gh pr list --head "$BRANCH_NAME" --json number --jq 'length')
          echo "pr_exists=$PR_COUNT" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        if: steps.check-pr.outputs.pr_exists == '0'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          PR_TITLE=$(echo "$BRANCH_NAME" | sed 's/-/ /g' | sed 's/^./\U&/' | sed 's/ ./\U&/g')
          gh pr create --title "$PR_TITLE" --body-file .github/pr-template.txt --base main --head "$BRANCH_NAME"

      - name: Comment on existing PR
        if: steps.check-pr.outputs.pr_exists != '0'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          PR_NUMBER=$(gh pr list --head "$BRANCH_NAME" --json number --jq '.[0].number')
          gh pr comment "$PR_NUMBER" --body "🔄 Branch has been updated with new commits."
